# Task: Split Monolith Repos into Architecture Components

> [!IMPORTANT]
> **STATUS: ON HOLD** — Do this AFTER the monorepo merge is complete and stable.

## Problem

The current repos are monoliths that bundle multiple architecture components together:

| Repo | Contains |
|---|---|
| `instagram-video-downloader` | CONTENT CREATION AUTOMATION API + PUBLISH API + SPAM API |
| `instagram-video-downloader-client` | SPAM UI (partly) + PUBLISH UI (partly) |

These need to be split into separate apps under `apps/` to match the target architecture from [01.init.md](file:///Users/niktverd/code/contento-monorepo/docs/01.init.md).

## Target State

```
apps/
├── spam-api/                    ← extracted from instagram-video-downloader
├── spam-ui/                     ← extracted from instagram-video-downloader-client
├── publish-api/                 ← extracted from instagram-video-downloader
├── publish-ui/                  ← extracted from instagram-video-downloader-client
├── content-automation-api/      ← extracted from instagram-video-downloader + temporal
├── contario/                    ← reels-creator (renamed)
└── reels-creator-backend/       ← may be removed if duplicate of temporal
```

## TODO (when unblocked)

- [ ] Audit `instagram-video-downloader` — identify code boundaries between SPAM API / PUBLISH API / CONTENT CREATION AUTOMATION API
- [ ] Audit `instagram-video-downloader-client` — identify code boundaries between SPAM UI / PUBLISH UI
- [ ] Check if `reels-creator-backend` functionality already exists in `instagram-video-downloader-temporal` or `instagram-video-downloader` — if so, remove
- [ ] Extract shared code into `packages/shared`
- [ ] Create separate apps for each architecture component
- [ ] Update CI/CD, Docker configs, and deployment scripts
- [ ] Rename `reels-creator` → `contario`
